<% content_for :nav  do %>
  <%= render "shared/nav" %>
<% end %>

<%= render "shared/errors" %>
<%= render "shared/messages" %>

<% content_for :page_header do %>
  <h2 class="page-header">Edit <%= h(@track.name[0..40]) %><%= @track.name.length >= 40 ? "..." : "" %> Track!</h2>
<% end %>

<%= render "form", track: @track %>
